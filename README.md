# CiscoAutoConnect
Create a file "credentials.txt" under "C:\Users\<username>\Documents" with the user name in the first and password in second line.

Execute "connect.bat" to connect.

Execute "disconnect.bat" to disconnect.

Optionally create desktop shortcuts to connect and disconnect shortcuts to bypass forbidden opening of .bat files.